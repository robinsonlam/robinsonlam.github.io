const roleSliderData = require(['/roleSlider.json']);

const initiateRoleSlider = () => {

	console.log('roleSliderData');

	let category = $(".webRole .category").text();
	let role = $(".webRole .role").text()

}

$(document).ready(initiateRoleSlider);
